RSpec.describe Bass::Auto::Fingering do
  it "has a version number" do
    expect(Bass::Auto::Fingering::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
